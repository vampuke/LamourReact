const host = "https://jessie.vampuck.com/"

export const LOGIN = host + "user/login";

export const LOGOUT = host + "user/logout";

export const JPUSH = host + "user/jpush";

export const NETWORK = host + "network";

export const VOUCHER = host + "voucher";

export const AVAIL_VOUCHER = host + "voucher/open"

export const WISH = host + "wish";

export const DEL_WISH = host + "wish/delete/"

export const ANNIV = host + "anniv";

export const FOOD = host + "food";

export const DEL_FOOD = host + 'food/delete/';

export const FLOWER = host + 'flower'